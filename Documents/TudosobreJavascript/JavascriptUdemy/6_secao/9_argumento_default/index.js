function potencia(base, exp=2) {
    return Math.pow(base, exp);
}
console.log(potencia(2)); // retorna 4, porque pod default o expoente é dois declarado na função
console.log(potencia(2,2));
console.log(potencia(2,3));

